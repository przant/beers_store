<%= form_with model: @promo, url: create_promo_admins_path, method: :post do |f| %>
  <% if @promo.errors.any? %>
    <div>
      <h2><%= pluralize(@promo.errors.count, "error") %></h2>
      <ul>
        <% @promo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="my-3">
    <%= f.label :name, class: 'form-label fs-4 fw-bold' %>
    <%= f.text_field :name, autocomplete: 'name', class: 'form-control' %>
  </div>
  <div class="my-3">
    <%= f.label :description, class: 'form-label fs-4 fw-bold' %>
    <%= f.text_area :description, autocomplete: 'description', class: 'form-control' %>
  </div>
  <div class="my-3" data-controller="range">
    <%= f.label :price, class: 'form-label fs-4 fw-bold' %>
    <div class="text-center">
      <%= f.range_field :price, in: 1..250, stetp: 1, value: 125, class: 'form-range', 'data-action': 'range#update', 'data-range-target': 'input' %>
      <%= f.label '$',  'data-range-target': 'label' %>
    </div>
  </div>
  <div class="d-flex mb-5 form-check align-items-center justify-content-center">
    <%= f.check_box :active, class: 'form-check-input mx-2' %>
    <%= f.label :active, class: 'form-check-label fs-5' %>
  </div>
  <div class="d-grid gap-2 mb-5">
    <%= f.submit "Add new promo", class: 'btn btn-primary btn-lg' %>
  </div>
<% end %>
